#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nulong longptr);

    int t;
    cin >> t;
    while (t--) {
        long long l, r, a;
        cin >> l >> r >> a;
        int maxi = r/a+r%a;
        cout<<maxi<<endl
    }
    
    return 0;
}
